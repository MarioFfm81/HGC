-if current_user && current_user.admin?
	=link_to 'New Post', :new_post_path
-@posts.each do |p|
	%div{:class => 'row'}
		%div{:class =>'col-xs-10 col-xs-push-1 post'}
			%b= p.title
			<br/>
			%p #{simple_format(p.content)}
		-if current_user && current_user.admin?
			%div{:class =>'col-xs-10 col-xs-push-1'}
				=link_to 'löschen', "/posts/#{p.id}", :method => :delete